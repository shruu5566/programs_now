# MongoDB Connection String
# Get this from MongoDB Atlas: Cluster > Connect > Connection String
# Replace <username>, <password>, and cluster name with your values
MONGO_URI=mongodb+srv://shrushtibp_db_user:jobtracker2@cluster0.zx78g9b.mongodb.net/job-tracker?retryWrites=true&w=majority

# JWT Secret Key
# Generate a random string of at least 32 characters for security
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here_min_32_characters

# Server Port
PORT=5000
