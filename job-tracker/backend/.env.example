# MongoDB Configuration
# ⚠️  CRITICAL: Never commit actual credentials to version control
# Get this from MongoDB Atlas: https://cloud.mongodb.com/v2/{YOUR_PROJECT_ID}#/security/database/users
MONGO_URI=

# JWT Secret (Generate a strong random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️  CRITICAL: Make this unique and never commit to version control
JWT_SECRET=

# Email Configuration (Gmail with App Password)
# 1. Enable 2FA on your Google account: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# ⚠️  CRITICAL: Use App Password (16 chars), not your regular password
EMAIL_USER=
EMAIL_PASS=

# Email From Display
EMAIL_FROM=Job Tracker <noreply@jobtracker.app>

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Server Port
PORT=5000
